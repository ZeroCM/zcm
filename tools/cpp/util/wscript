#! /usr/bin/env python
# encoding: utf-8

def build(ctx):
    ctx.stlib(target = 'zcm_tools_util',
              use = ['default', 'zcm', 'zcm_tools'],
              lib = 'dl',
              source = ctx.path.ant_glob('*.cpp'))
