// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "b.h"

static int __b_hash_computed;
static uint64_t __b_hash;

uint64_t __b_hash_recursive(const __zcm_hash_ptr *p)
{
    const __zcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __b_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__b_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xf53820e02f1e593fLL
         + __int32_t_hash_recursive(&cp)
         + __a_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __b_get_hash(void)
{
    if (!__b_hash_computed) {
        __b_hash = (int64_t)__b_hash_recursive(NULL);
        __b_hash_computed = 1;
    }

    return __b_hash;
}

int __b_encode_array(void *buf, int offset, int maxlen, const b *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].val2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __a_encode_array(buf, offset + pos, maxlen - pos, &(p[element].a_), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int b_encode(void *buf, int offset, int maxlen, const b *p)
{
    int pos = 0, thislen;
    int64_t hash = __b_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __b_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __b_encoded_array_size(const b *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int32_t_encoded_array_size(&(p[element].val2), 1);

        size += __a_encoded_array_size(&(p[element].a_), 1);

    }
    return size;
}

int b_encoded_size(const b *p)
{
    return 8 + __b_encoded_array_size(p, 1);
}

int __b_decode_array(const void *buf, int offset, int maxlen, b *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].val2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __a_decode_array(buf, offset + pos, maxlen - pos, &(p[element].a_), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __b_decode_array_cleanup(b *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int32_t_decode_array_cleanup(&(p[element].val2), 1);

        __a_decode_array_cleanup(&(p[element].a_), 1);

    }
    return 0;
}

int b_decode(const void *buf, int offset, int maxlen, b *p)
{
    int pos = 0, thislen;
    int64_t hash = __b_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __b_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int b_decode_cleanup(b *p)
{
    return __b_decode_array_cleanup(p, 1);
}

int __b_clone_array(const b *p, b *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int32_t_clone_array(&(p[element].val2), &(q[element].val2), 1);

        __a_clone_array(&(p[element].a_), &(q[element].a_), 1);

    }
    return 0;
}

b *b_copy(const b *p)
{
    b *q = (b*) malloc(sizeof(b));
    __b_clone_array(p, q, 1);
    return q;
}

void b_destroy(b *p)
{
    __b_decode_array_cleanup(p, 1);
    free(p);
}

int b_publish(zcm_t *lc, const char *channel, const b *p)
{
      int max_data_size = b_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = b_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (lc, channel, (char *)buf, (size_t)data_size);
      free (buf);
      return status;
}

struct _b_subscription_t {
    b_handler_t user_handler;
    void *userdata;
    zcm_sub_t *z_sub;
};
static
void b_handler_stub (const zcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    b p;
    memset(&p, 0, sizeof(b));
    status = b_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding b!!!\n", status);
        return;
    }

    b_subscription_t *h = (b_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    b_decode_cleanup (&p);
}

b_subscription_t* b_subscribe (zcm_t *zcm,
                    const char *channel,
                    b_handler_t f, void *userdata)
{
    b_subscription_t *n = (b_subscription_t*)
                       malloc(sizeof(b_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              b_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg b ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int b_unsubscribe(zcm_t *zcm, b_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe b_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

