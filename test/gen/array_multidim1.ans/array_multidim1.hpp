/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by zcm-gen
 **/

#include <zcm/zcm_coretypes.h>

#ifndef __array_multidim1_hpp__
#define __array_multidim1_hpp__

#include <vector>


class array_multidim1
{
    public:
        double     arr1[2][3][4][5];

        int32_t    i1;

        int16_t    i2;

        std::vector< std::vector< std::vector< std::vector< double > > > > arr2;

        std::vector< std::vector< std::vector< std::vector< double > > > > arr3;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "array_multidim1"
         */
        inline static const char* getTypeName();

        // ZCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __zcm_hash_ptr *p);
};

int array_multidim1::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int array_multidim1::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int array_multidim1::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t array_multidim1::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* array_multidim1::getTypeName()
{
    return "array_multidim1";
}

int array_multidim1::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    for (int a0 = 0; a0 < 2; a0++) {
        for (int a1 = 0; a1 < 3; a1++) {
            for (int a2 = 0; a2 < 4; a2++) {
                tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->arr1[a0][a1][a2][0], 5);
                if(tlen < 0) return tlen; else pos += tlen;
            }
        }
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->i1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->i2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->i1; a0++) {
        for (int a1 = 0; a1 < 3; a1++) {
            for (int a2 = 0; a2 < this->i2; a2++) {
                tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->arr2[a0][a1][a2][0], 5);
                if(tlen < 0) return tlen; else pos += tlen;
            }
        }
    }

    if(this->i1 > 0) {
        for (int a0 = 0; a0 < 2; a0++) {
            for (int a1 = 0; a1 < this->i2; a1++) {
                for (int a2 = 0; a2 < 4; a2++) {
                    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->arr3[a0][a1][a2][0], this->i1);
                    if(tlen < 0) return tlen; else pos += tlen;
                }
            }
        }
    }

    return pos;
}

int array_multidim1::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    for (int a0 = 0; a0 < 2; a0++) {
        for (int a1 = 0; a1 < 3; a1++) {
            for (int a2 = 0; a2 < 4; a2++) {
                tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->arr1[a0][a1][a2][0], 5);
                if(tlen < 0) return tlen; else pos += tlen;
            }
        }
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->i1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->i2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->arr2.resize(this->i1);
    for (int a0 = 0; a0 < this->i1; a0++) {
        this->arr2[a0].resize(3);
        for (int a1 = 0; a1 < 3; a1++) {
            this->arr2[a0][a1].resize(this->i2);
            for (int a2 = 0; a2 < this->i2; a2++) {
                if(5) {
                    this->arr2[a0][a1][a2].resize(5);
                    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->arr2[a0][a1][a2][0], 5);
                    if(tlen < 0) return tlen; else pos += tlen;
                }
            }
        }
    }

    this->arr3.resize(2);
    for (int a0 = 0; a0 < 2; a0++) {
        this->arr3[a0].resize(this->i2);
        for (int a1 = 0; a1 < this->i2; a1++) {
            this->arr3[a0][a1].resize(4);
            for (int a2 = 0; a2 < 4; a2++) {
                if(this->i1) {
                    this->arr3[a0][a1][a2].resize(this->i1);
                    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->arr3[a0][a1][a2][0], this->i1);
                    if(tlen < 0) return tlen; else pos += tlen;
                }
            }
        }
    }

    return pos;
}

int array_multidim1::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += 2 * 3 * 4 * __double_encoded_array_size(NULL, 5);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += this->i1 * 3 * this->i2 * __double_encoded_array_size(NULL, 5);
    enc_size += 2 * this->i2 * 4 * __double_encoded_array_size(NULL, this->i1);
    return enc_size;
}

uint64_t array_multidim1::_computeHash(const __zcm_hash_ptr *)
{
    uint64_t hash = (uint64_t)0x2ec8cd78def3a21eLL;
    return (hash<<1) + ((hash>>63)&1);
}

#endif
