// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "array_multidim1.h"

static int __array_multidim1_hash_computed;
static uint64_t __array_multidim1_hash;

uint64_t __array_multidim1_hash_recursive(const __zcm_hash_ptr *p)
{
    const __zcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __array_multidim1_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__array_multidim1_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x2ec8cd78def3a21eLL
         + __double_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __array_multidim1_get_hash(void)
{
    if (!__array_multidim1_hash_computed) {
        __array_multidim1_hash = (int64_t)__array_multidim1_hash_recursive(NULL);
        __array_multidim1_hash_computed = 1;
    }

    return __array_multidim1_hash;
}

int __array_multidim1_encode_array(void *buf, int offset, int maxlen, const array_multidim1 *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].arr1[a][b][c], 5);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].i1), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].i2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        { int a;
        for (a = 0; a < p[element].i1; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < p[element].i2; c++) {
                    thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].arr2[a][b][c], 5);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < p[element].i2; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].arr3[a][b][c], p[element].i1);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

    }
    return pos;
}

int array_multidim1_encode(void *buf, int offset, int maxlen, const array_multidim1 *p)
{
    int pos = 0, thislen;
    int64_t hash = __array_multidim1_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __array_multidim1_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __array_multidim1_encoded_array_size(const array_multidim1 *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    size += __double_encoded_array_size(p[element].arr1[a][b][c], 5);
                }
                }
            }
            }
        }
        }

        size += __int32_t_encoded_array_size(&(p[element].i1), 1);

        size += __int16_t_encoded_array_size(&(p[element].i2), 1);

        { int a;
        for (a = 0; a < p[element].i1; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < p[element].i2; c++) {
                    size += __double_encoded_array_size(p[element].arr2[a][b][c], 5);
                }
                }
            }
            }
        }
        }

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < p[element].i2; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    size += __double_encoded_array_size(p[element].arr3[a][b][c], p[element].i1);
                }
                }
            }
            }
        }
        }

    }
    return size;
}

int array_multidim1_encoded_size(const array_multidim1 *p)
{
    return 8 + __array_multidim1_encoded_array_size(p, 1);
}

int __array_multidim1_decode_array(const void *buf, int offset, int maxlen, array_multidim1 *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].arr1[a][b][c], 5);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].i1), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].i2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        p[element].arr2 = (double****) zcm_malloc(sizeof(double***) * p[element].i1);
        { int a;
        for (a = 0; a < p[element].i1; a++) {
            p[element].arr2[a] = (double***) zcm_malloc(sizeof(double**) * 3);
            { int b;
            for (b = 0; b < 3; b++) {
                p[element].arr2[a][b] = (double**) zcm_malloc(sizeof(double*) * p[element].i2);
                { int c;
                for (c = 0; c < p[element].i2; c++) {
                    p[element].arr2[a][b][c] = (double*) zcm_malloc(sizeof(double) * 5);
                    thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].arr2[a][b][c], 5);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

        p[element].arr3 = (double****) zcm_malloc(sizeof(double***) * 2);
        { int a;
        for (a = 0; a < 2; a++) {
            p[element].arr3[a] = (double***) zcm_malloc(sizeof(double**) * p[element].i2);
            { int b;
            for (b = 0; b < p[element].i2; b++) {
                p[element].arr3[a][b] = (double**) zcm_malloc(sizeof(double*) * 4);
                { int c;
                for (c = 0; c < 4; c++) {
                    p[element].arr3[a][b][c] = (double*) zcm_malloc(sizeof(double) * p[element].i1);
                    thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].arr3[a][b][c], p[element].i1);
                    if (thislen < 0) return thislen; else pos += thislen;
                }
                }
            }
            }
        }
        }

    }
    return pos;
}

int __array_multidim1_decode_array_cleanup(array_multidim1 *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    __double_decode_array_cleanup(p[element].arr1[a][b][c], 5);
                }
                }
            }
            }
        }
        }

        __int32_t_decode_array_cleanup(&(p[element].i1), 1);

        __int16_t_decode_array_cleanup(&(p[element].i2), 1);

        { int a;
        for (a = 0; a < p[element].i1; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < p[element].i2; c++) {
                    __double_decode_array_cleanup(p[element].arr2[a][b][c], 5);
                    if (p[element].arr2[a][b][c]) free(p[element].arr2[a][b][c]);
                }
                }
                if (p[element].arr2[a][b]) free(p[element].arr2[a][b]);
            }
            }
            if (p[element].arr2[a]) free(p[element].arr2[a]);
        }
        }
        if (p[element].arr2) free(p[element].arr2);

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < p[element].i2; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    __double_decode_array_cleanup(p[element].arr3[a][b][c], p[element].i1);
                    if (p[element].arr3[a][b][c]) free(p[element].arr3[a][b][c]);
                }
                }
                if (p[element].arr3[a][b]) free(p[element].arr3[a][b]);
            }
            }
            if (p[element].arr3[a]) free(p[element].arr3[a]);
        }
        }
        if (p[element].arr3) free(p[element].arr3);

    }
    return 0;
}

int array_multidim1_decode(const void *buf, int offset, int maxlen, array_multidim1 *p)
{
    int pos = 0, thislen;
    int64_t hash = __array_multidim1_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __array_multidim1_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int array_multidim1_decode_cleanup(array_multidim1 *p)
{
    return __array_multidim1_decode_array_cleanup(p, 1);
}

int __array_multidim1_clone_array(const array_multidim1 *p, array_multidim1 *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        { int a;
        for (a = 0; a < 2; a++) {
            { int b;
            for (b = 0; b < 3; b++) {
                { int c;
                for (c = 0; c < 4; c++) {
                    __double_clone_array(p[element].arr1[a][b][c], q[element].arr1[a][b][c], 5);
                }
                }
            }
            }
        }
        }

        __int32_t_clone_array(&(p[element].i1), &(q[element].i1), 1);

        __int16_t_clone_array(&(p[element].i2), &(q[element].i2), 1);

        q[element].arr2 = (double****) zcm_malloc(sizeof(double***) * q[element].i1);
        { int a;
        for (a = 0; a < p[element].i1; a++) {
            q[element].arr2[a] = (double***) zcm_malloc(sizeof(double**) * 3);
            { int b;
            for (b = 0; b < 3; b++) {
                q[element].arr2[a][b] = (double**) zcm_malloc(sizeof(double*) * q[element].i2);
                { int c;
                for (c = 0; c < p[element].i2; c++) {
                    q[element].arr2[a][b][c] = (double*) zcm_malloc(sizeof(double) * 5);
                    __double_clone_array(p[element].arr2[a][b][c], q[element].arr2[a][b][c], 5);
                }
                }
            }
            }
        }
        }

        q[element].arr3 = (double****) zcm_malloc(sizeof(double***) * 2);
        { int a;
        for (a = 0; a < 2; a++) {
            q[element].arr3[a] = (double***) zcm_malloc(sizeof(double**) * q[element].i2);
            { int b;
            for (b = 0; b < p[element].i2; b++) {
                q[element].arr3[a][b] = (double**) zcm_malloc(sizeof(double*) * 4);
                { int c;
                for (c = 0; c < 4; c++) {
                    q[element].arr3[a][b][c] = (double*) zcm_malloc(sizeof(double) * q[element].i1);
                    __double_clone_array(p[element].arr3[a][b][c], q[element].arr3[a][b][c], p[element].i1);
                }
                }
            }
            }
        }
        }

    }
    return 0;
}

array_multidim1 *array_multidim1_copy(const array_multidim1 *p)
{
    array_multidim1 *q = (array_multidim1*) malloc(sizeof(array_multidim1));
    __array_multidim1_clone_array(p, q, 1);
    return q;
}

void array_multidim1_destroy(array_multidim1 *p)
{
    __array_multidim1_decode_array_cleanup(p, 1);
    free(p);
}

int array_multidim1_publish(zcm_t *lc, const char *channel, const array_multidim1 *p)
{
      int max_data_size = array_multidim1_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = array_multidim1_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (lc, channel, (char *)buf, (size_t)data_size);
      free (buf);
      return status;
}

struct _array_multidim1_subscription_t {
    array_multidim1_handler_t user_handler;
    void *userdata;
    zcm_sub_t *z_sub;
};
static
void array_multidim1_handler_stub (const zcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    array_multidim1 p;
    memset(&p, 0, sizeof(array_multidim1));
    status = array_multidim1_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding array_multidim1!!!\n", status);
        return;
    }

    array_multidim1_subscription_t *h = (array_multidim1_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    array_multidim1_decode_cleanup (&p);
}

array_multidim1_subscription_t* array_multidim1_subscribe (zcm_t *zcm,
                    const char *channel,
                    array_multidim1_handler_t f, void *userdata)
{
    array_multidim1_subscription_t *n = (array_multidim1_subscription_t*)
                       malloc(sizeof(array_multidim1_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              array_multidim1_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg array_multidim1 ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int array_multidim1_unsubscribe(zcm_t *zcm, array_multidim1_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe array_multidim1_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

