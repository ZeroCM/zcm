#! /usr/bin/env python
# encoding: utf-8

import os

def build(ctx):
    ctx(name = 'pyzcm',
        target = 'zerocm',
        features = 'c cshlib pyext',
        source = 'zerocm.pyx',
        use = 'zcm').init_pyext()
